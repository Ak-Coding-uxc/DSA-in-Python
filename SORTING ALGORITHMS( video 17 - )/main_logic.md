# Bubble Sort 
- swap consecutive value(joh ek ke baad ek aati h).push max value to the last  


# Selection Sort
- Find MINIMUM VALUE and interchane with current outer loop index value.
- for ex:- outer loop is used to select current value and  
inner loop is used to select minimum value.  

# Insertion Sort
- if current element is small then previous element then interchange this element with previous element.   
- see code if you not understand.  
